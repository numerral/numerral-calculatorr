// AuthorBadge — disabled site-wide (returns null)
interface AuthorBadgeProps {
    categoryKey: string;
    updatedDate?: string;
}

export default function AuthorBadge(_props: AuthorBadgeProps) {
    return null;
}
